'use strict';

/**
 * adjusted-entries-record router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::adjusted-entries-record.adjusted-entries-record');
