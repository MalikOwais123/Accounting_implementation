'use strict';

/**
 * adjusted-entries-record service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::adjusted-entries-record.adjusted-entries-record');
